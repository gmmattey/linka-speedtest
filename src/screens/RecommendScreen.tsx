// Tela removida no refator de arquitetura 2026-05.
// As recomendações deixaram de ser uma tela dedicada — agora aparecem como
// itens `[problema] → [ação]` dentro do card `.lk-result__combined` da
// ResultScreen, derivados de `utils/diagnosisItems.ts`.
// Pendência: `git rm src/screens/RecommendScreen.{tsx,css}` no próximo commit.
export {};
